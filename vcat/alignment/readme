Script to do the alingment: align_imagesEHTim.py:
- Uses the ehtim library: pip install ehtim

Additional requirements: mpl_toolkits, skimage, scipy, numpy

Example-script to apply alignment to images: example_alignImages.py
